# TODO - Update component-previews.json with real preview components

- [x] Read all real preview components from `components/preview/`
- [x] Analyze current `component-previews.json` structure and consuming pages
- [x] Rewrite `component-previews.json` with 13 real carousel entries
- [x] Set `size: "full"` for all entries
- [x] Set correct `path` and `file` pointing to `components/preview/`
- [x] Keep previewImage URLs unchanged
- [x] Test pages render correctly

